<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <application
        android:allowBackup="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/dinedash"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/dinedash"
        android:supportsRtl="true"
        android:theme="@style/Theme.DineDash"
        tools:ignore="MissingClass">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.MenuActivity" />
        <activity android:name=".ui.PlatformActivity" />
        <activity android:name=".ui.PaymentActivity" />
        <activity android:name=".ui.CardActivity" />
        <activity android:name=".ui.RegisterActivity" />
        <activity android:name=".ui.ThankYouActivity" />
        <activity android:name=".ui.AdminLoginActivity" />
        <activity android:name=".ui.AdminActivity" />
        <activity android:name=".ui.AdminTableActivity" />
        <activity android:name=".ui.MenuDetailsActivity" />
        <activity android:name=".ui.BkashActivity"/>
        <activity android:name=".ui.CashPaymentActivity"/>










    </application>

<!--    <uses-permission android:name="android.permission.INTERNET" />-->
</manifest>